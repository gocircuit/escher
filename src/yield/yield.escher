
// For every circuit incoming on valve OfCircuit, YieldValues produces
// a sequence enumerating the gate values of the argument circuit
// over the default valve.
//
YieldValues { // : :Frame :Control
}

YieldNames { // : :Frame :Control
}

TestYieldValues {
}

// yieldTestValues streams a list of values on its default valve.
// The last streamed value is 3.
yieldTestValues {
	t YieldValues
	t: = { 1, 2, 3 }
	t:Frame = :Frame
	t:Control = :Control
}
